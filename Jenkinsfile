pipeline {
    agent { label 'ec2_node' }

   stages {
      stage('install plugins') {
         steps {
            echo "${WORKSPACE}"
            tee input_plugin.txt<<EOF 
            
         }
      }
      stage(publish){
steps{
    echo "publish stage"
}
      }
   }
}
